server:
  port: 8085

spring:
  application:
    name: CONFIG-SERVER

  cloud:
    config:
      server:
        git:
          uri: https://github.com/RohitRawat2001/micro-service-configuration
          clone-on-start: true

#eureka:
#  client:
#    service-url:
#      defaultZone: http://localhost:8761/eureka/  # Eureka server URL for service registration and discovery
#    fetch-registry: true
#    register-with-eureka: true
#  instance:
#    prefer-ip-address: true
